package com.example.javaddit.features.vote.entity;

/**
 * Enum representing the type of vote a user can cast.
 * UPVOTE increases the score by 1, DOWNVOTE decreases it by 1.
 */
public enum VoteType {
    UPVOTE,
    DOWNVOTE
}
